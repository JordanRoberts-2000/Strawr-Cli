pub fn handle_img(path: Option<String>) {
    match path {
        Some(path) => {
            // if its a file
            // if its a folder
            // if its a url
        }
        None => {
            // please specify an image
        }
    }
}

// -
